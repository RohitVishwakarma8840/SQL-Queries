CREATE TABLE temp1(
id INT,
name VARCHAR(100),
age INT,
city VARCHAR(100),
PRIMARY KEY(id,name)  -- primary key kaa combination kabhi bhi duplicate nahi ho saktaa
-- that's the important point in selecting the terminal 


);

SELECT * FROM temp1;
